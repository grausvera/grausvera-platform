{
  "name": "grausvera-platform",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "npm@11.19.0",
  "engines": {
    "node": "24.20.0",
    "npm": "11.19.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @grausvera/database && npm run build --workspace @grausvera/operations && npm run build --workspace @grausvera/public-content && npm run build --workspace @grausvera/shared && npm run build --workspace @grausvera/web && npm run build --workspace @grausvera/worker",
    "db:migrate": "npm run build --workspace @grausvera/database && node --env-file-if-exists=.env packages/database/dist/migrate.js",
    "format:check": "biome format .",
    "job:enqueue:synthetic": "npm run build --workspace @grausvera/worker && npm run enqueue:synthetic --workspace @grausvera/worker",
    "lint": "biome lint .",
    "operator:provision": "node --experimental-strip-types --env-file-if-exists=.env apps/web/scripts/provision-operator.ts",
    "test:contracts": "vitest run --config vitest.contracts.config.ts",
    "test:e2e": "playwright test",
    "test:eval": "vitest run --config vitest.eval.config.ts",
    "test:integration": "node --env-file-if-exists=.env node_modules/vitest/vitest.mjs run --config vitest.integration.config.ts",
    "test:recovery": "node scripts/pending-suite.mjs test:recovery TASK-GP-061",
    "test:security": "node --env-file-if-exists=.env node_modules/vitest/vitest.mjs run --config vitest.security.config.ts",
    "test:unit": "vitest run",
    "typecheck": "npm run typecheck --workspace @grausvera/database && npm run typecheck --workspace @grausvera/operations && npm run typecheck --workspace @grausvera/public-content && npm run typecheck --workspace @grausvera/shared && npm run typecheck --workspace @grausvera/web && npm run typecheck --workspace @grausvera/worker"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.12",
    "@playwright/test": "1.63.0",
    "vitest": "5.0.0"
  }
}
